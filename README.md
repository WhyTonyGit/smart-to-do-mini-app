# MindFlow bot

LLM-поддерживаемый бот-ассистент для трекинга задач и привычек.  
Пользователь пишет текстом «завтра в 8 утра позвонить маме срочно» — бот парсит это в задачу, сохраняет в БД, напоминает и строит статистику по неделе.


## Презентация проекта
Для полного просмотра кликните по картинке

<div align="center">
  <a href="./MindFlow.pdf">
    <img src="./MindFlow-preview.png" alt="MindFlow Presentation" width="800">
  </a>
</div>

## Основные возможности

- ✅ Создание задач на естественном русском языке (через LLM в Ollama)
- 🌱 Трекер привычек с интервалами (каждый день / будни / выходные / конкретные дни недели)
- 📅 Списки задач и привычек: на сегодня, на неделю, все активные
- 📊 Еженедельные сводки по задачам и привычкам
- 🔥 Подсчёт серий по привычкам (streaks)
- 💌 Ежедневные мотивационные сообщения через LLM
- 🤖 Интеграция с Max Bot API (бот общается с пользователем через чат)

---

## Технологический стек

- Java + Spring Boot
- PostgreSQL — основная БД
- Redis — кеш и временное хранение «черновиков» задач/привычек
- Ollama + модель `qwen2.5:0.5b` — LLM для NLP и мотивации
- Docker / Docker Compose
- Max Bot API — канал доставки сообщений пользователю

---

## Архитектура (docker-сервисы)

`docker-compose.yml` поднимает несколько сервисов:

- **db** — PostgreSQL `smarttodo`
- **redis** — Redis 7
- **ollama** — Ollama сервер (порт `11434`)
- **ollama-pull** — *one-shot* контейнер, который:
    - ждёт готовности `ollama`
    - пуллит модель `qwen2.5:0.5b`
    - делает «прогрев» модели и **завершается**
- **app** — основной Spring Boot сервис (`smarttodo-app`, порт `8080`)

> ВАЖНО: как только контейнер `ollama-pull` отработал и перешёл в состояние `Exited (0)` — модель загружена, и приложение готово к работе.

---

## Предварительные требования

- Docker ≥ 20.x
- Docker Compose v2 (команда `docker compose`)
- Открытые порты:
    - `5432` — PostgreSQL
    - `6379` — Redis
    - `11434` — Ollama
    - `8080` — HTTP API приложения

---

### 1. Max Bot API (`secrets-max.env`)

В корне проекта должен быть файл `secrets-max.env` 
(Необходимо добавить в .gitignore этот файл, чтобы не передавать информацию 3 лицам):

    MAX_BOT_TOKEN=your_max_bot_token_here
    MAX_API_BASE_URL=https://api.max.example.com

Эти значения используются в конфигурации `MaxConfig` и `MaxProps`.

### 2. База данных и Redis

В `docker-compose.yml` уже заданы параметры подключения для приложения:

    SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/smarttodo
    SPRING_DATASOURCE_USERNAME=smarttodo
    SPRING_DATASOURCE_PASSWORD=smarttodo

    SPRING_DATA_REDIS_HOST=redis
    SPRING_DATA_REDIS_PORT=6379

Дополнительной ручной настройки не требуется.  
Каталог `./secrets-db` монтируется как хранилище конфигов/секретов (может быть пустым в базовом сценарии).

### 3. Настройки Ollama / LLM

Переменные из `docker-compose.yml`:

    OLLAMA_API_BASE_URL=http://ollama:11434
    OLLAMA_API_MODEL=qwen2.5:0.5b
    OLLAMA_API_TIMEOUT_SECONDS=120
    REMINDER_MODEL=reminder-title

Они мапятся на `OllamaProps` и используются в `OllamaClient` и сервисах:

- `NlpService` — парсинг текста в задачи
- `MotivationService` — генерация мотивационных сообщений

---

## Запуск через Docker Compose

Команда:

    docker compose up -d --build

Что происходит:

- Собирается образ приложения (`smarttodo-app`)
- Поднимаются контейнеры: `db`, `redis`, `ollama`, `app`, а также `ollama-pull`

После этого важно дождаться завершения `ollama-pull`:

    docker compose ps

Нужно состояние вида:

    smarttodo-ollama-pull   Exit 0
    smarttodo-ollama        Up
    smarttodo-db            Up
    smarttodo-redis         Up
    smarttodo-app           Up

> Правило проекта: как только `ollama-pull` остановился с кодом `Exit 0` — модель (`qwen2.5:0.5b`) скачана и прогрета, LLM готов, приложение может полноценно отвечать.

## Проверка работоспособности

### Health-чек приложения

В приложении есть простой health-эндпоинт:

    GET http://localhost:8080/webhook/max/health

Проверка через `curl`:

    curl http://localhost:8080/webhook/max/health

Ожидаемый ответ:

    OK

---

## Примеры запросов к API (через командную строку)

Ниже примеры, которые можно использовать как smoke-тест для LLM-функций.

### 1. Генерация мотивационного сообщения

**Эндпоинт:** `POST /motivation/message`  
**Контроллер:** `MotivationController`

Запрос:

    curl -X POST http://localhost:8080/motivation/message \
      -H "Content-Type: application/json" \
      -d '{"streakDays": 7}'

Пример ответа:

    {
      "message": "Ты уже 7 дней подряд держишься, это очень сильный результат. Продолжай в том же духе."
    }

Здесь:

- Вход: `streakDays` — длина стрика
- `MotivationService` формирует пользовательский промпт
- Передаёт его в `OllamaClient.chatText(...)`
- Результат заворачивается в `MotivationResponse`

### 2. Парсинг текста в задачи (NLP)

**Эндпоинт:** `POST /nlp/parse`  
**Контроллер:** `NlpController`

Запрос:

    curl -X POST http://localhost:8080/nlp/parse \
      -H "Content-Type: application/json" \
      -d '{
        "text": "Позвонить маме завтра в 8 утра срочно"
      }'

Пример структуры ответа:

    {
      "tasks": [
        {
          "title": "Позвонить маме",
          "description": null,
          "datetime": "dd.MM.yyyy HH:mm",
          "priority": "high",
          "splitOf": null
        }
      ]
    }

Логика:

- `NlpService` генерирует system prompt с правилами:
    - «сегодня» / «завтра» → конкретные даты
    - «утром» / «вечером» и т.д. → дефолтное время
    - «срочно» / «очень важно» → `priority = "high"`
- `OllamaClient.chatExtractJson(...)` стримит ответ от модели, накапливает JSON и маппит в `ParseResult`.

---

## Интеграция с Max Bot API

- Входящие апдейты Max приходят на:

      POST /webhook/max

  (контроллер `MaxWebhookController`)

- Тело запроса маппится в сущность `Update`, которая:
    - умеет определять тип события (`message_created`, `message_callback`)
    - предоставляет удобные методы `isText()`, `isCallback()`, `chatId()`, `userId()` и т.п.

- Далее `UpdateRouter` (не показан здесь, но используется в контроллере) маршрутизирует:
    - команды (`/start` и др.)
    - текстовые сообщения (создание/редактирование задач и привычек)
    - callback-и от инлайн-кнопок

- Ответы пользователю отправляются через `MaxApi`:
    - `postMessage(...)` → `POST /messages?chat_id=...`
    - `editMessage(...)` → `PUT /messages?message_id=...`

`MaxConfig` собирает `WebClient` с:

- `max.api.base-url`
- заголовком `Authorization: <MAX_BOT_TOKEN>`
- логированием запросов и ответов
- sanity-check при старте: без `MAX_BOT_TOKEN` и `max.api.base-url` приложение не поднимется.

---

## Тестирование бота
Чтобы протестировать бота, ему нужен **публично доступный URL**, по которому платформа сможет доставлять запросы.

Есть два варианта:

1. **Развернуть бота на сервере**  
   Запустите приложение на любом сервере с публичным доступом по HTTPS и получите URL вида, например:  
   `https://my-bot.example.com`.
2. **Запустить бота локально и пробросить порт в интернет**  
   Запустите бота на локальной машине (например, на порту `8000`), а затем поднимите туннель с помощью `ngrok` (или аналога):

   ```bash
   ngrok http 8000
   ```
    В результате вы получите публичный URL вида: https://<random>.ngrok-free.app, который будет проксировать запросы на ваш локальный сервер.
   
### Регистрация вебхука в платформе
После того как у вас есть публичный URL (сервер или ngrok), его нужно зарегистрировать в платформе с помощью HTTP-запроса:

Метод: 'POST'
URL: 'https://platform-api.max.ru/subscriptions'

В запросе:
- в заголовках передаётся токен бота (например, в виде заголовка Authorization),
- в теле передаётся публичный URL бота.

---

Файл с лицензией на использование Qwen2.5-0.5B – LICENSE-Qwen2.5

This product uses Qwen2.5-0.5B, licensed under the Apache License, Version 2.0.
© Alibaba Cloud.