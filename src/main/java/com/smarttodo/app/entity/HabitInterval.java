package com.smarttodo.app.entity;

public enum HabitInterval {
    EVERY_DAY,
    EVERY_WEEK,
    EVERY_MONTH,
    EVERY_WEEKEND,
    EVERY_WEEKDAY,

    EVERY_SUNDAY,
    EVERY_MONDAY,
    EVERY_TUESDAY,
    EVERY_THURSDAY,
    EVERY_WEDNESDAY,
    EVERY_FRIDAY,
    EVERY_SATURDAY,
}
